<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.andmebaas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applepen"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/applepen"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.user.andmebaas.LoginActivity" />
        <activity android:name="com.example.user.andmebaas.PasswordActivity" />
        <activity android:name="com.example.user.andmebaas.RegisterActivity" />
        <activity android:name="com.example.user.andmebaas.UserProfileActivity" />
        <activity android:name="com.example.user.andmebaas.LogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.user.andmebaas.MainAndmed">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.user.andmebaas.KuvaDetailid"></activity>
    </application>

</manifest>